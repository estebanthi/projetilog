class Book {
    constructor(title, author) {
        this.title = title;
        this.author = author;
    }
    toString() {
        return `${this.title} par ${this.author}`;
    }
}
//# sourceMappingURL=book.js.map